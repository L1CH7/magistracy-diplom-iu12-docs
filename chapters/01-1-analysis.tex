\section{Сравнительный обзор подходов к агент-ориентированному моделированию и навигации}

При проектировании навигационных комплексов для современных мегаполисов одной из наиболее острых проблем является выбор правильной архитектурной парадигмы управления транспортными потоками. Дорожная сеть большой размерности (например, Московская агломерация содержит порядка 600 тысяч сегментов дорог и 1,5 миллиона возможных маневров) ставит перед исследователями серьезные вычислительные вызовы. В рамках данной научно-исследовательской работы был проведен сравнительный анализ применимости современных математических и алгоритмических подходов к решению задачи агент-ориентированной навигации и планирования транспортных систем \cite{yakimov2013planning, yakimov2022planning_visum}.

Большинство передовых парадигм (2022–2026 гг.) математически состоятельны, однако их практическая реализация в условиях жестких ограничений по времени отклика (латентности) и объему оперативной памяти затруднена. Результаты отсева представлены в таблице \ref{tab:paradigms_comparison}.

\begin{table}[htbp]
\centering
\small
\renewcommand{\arraystretch}{1.2}
\caption{Анализ применимости современных алгоритмических парадигм маршрутизации}
\label{tab:paradigms_comparison}
\begin{tabularx}{\linewidth}{|p{3.5cm}|X|X|}
\hline
Парадигма & Применимые концепции (Теоретическая польза) & Аппаратный или алгоритмический барьер (Ограничения) \\ \hline
Игры среднего поля (Mean Field Games, MFG) & Сведение транспортных потоков к строгой непрерывной гидродинамике (сопряженные дифференциальные уравнения) \cite{seaports_mean_field, huang2007large}. & Сложность решения сопряженной системы прямых-обратных дифференциальных уравнений Гамильтона~-- Якоби~-- Беллмана (Hamilton-Jacobi-Bellman, HJB) и Фоккера~-- Планка~-- Колмогорова (Fokker-Planck-Kolmogorov, FPK). Требуется допущение стационарности, нарушающееся в реальном времени. \\ \hline
Непрерывный совместный поиск путей для множества агентов (Multi-Agent Path Finding, MAPF) / алгоритм PIBT (Priority Inheritance Bottleneck Tracking) & Временная сложность $O(1)$ для однородных сеток (приоритетный сдвиг на роботизированных складах) \cite{stern2019mapf, okumura2019pibt}. & Неприменимость допущения о синхронности шагов на реальном асинхронном графе дорог с разбросом длин ребер. Алгоритмическая NP-трудность (класс сложности NP ~-- недетерминированное полиномиальное время) поиска оптимального решения. \\ \hline
Мультиагентное обучение с подкреплением (Multi-Agent Reinforcement Learning, MARL) & Возможность индивидуальной адаптивности агентов к сложным нелинейным заторам \cite{hernandez2017survey}. & Нестационарность среды (цель обучения постоянно смещается), шум одновременного исследования и плохая масштабируемость на крупных дорожных сетях. \\ \hline
Тензорная квадратичная оптимизация без ограничений (Quadratic Unconstrained Binary Optimization, QUBO) & Математически гарантированный поиск системного глобального оптимума \cite{feld2019hybrid}. & Жесткие физические ограничения квантовых процессоров по числу кубитов и разреженной топологии связей (Chimera-граф), требующие эвристической декомпозиции (QBSolv). Высокая задержка вызовов квантового сопроцессора (Quantum Processing Unit, QPU) по сравнению с локальной эмуляцией на центральном процессоре. \\ \hline
Физически информированные графовые нейронные сети (Physics-Informed Neural Network, PINN / Graph Neural Network, GNN) & Высокая скорость инференса (прямого прохода) для получения готовых маршрутов \cite{luo2024stg4traffic}. & Ограничения выразительной способности в локальных моделях и абсолютная неспособность к онлайн-адаптации: изменение статической топологии (перекрытие дороги) требует полного переобучения модели на GPU-кластере \cite{garg2020generalization}. \\ \hline
Пространственное хеширование & Концепция Z-кривой (коды Мортона) гарантирует, что соседние 2D-узлы лежат в одной кэш-линии процессора \cite{morton1966computer}. & Маршрутизация агентов исключительно по геометрическим ячейкам (Quadtrees) неэффективна на графах со строгими правилами движения. \\ \hline
Роевой интеллект (Стигмергия) & Цифровые феромоны соответствуют концепции 5-минутных интервалов резервирования (чтение уровня затора за $O(1)$) \cite{deepa2016swarm}. & Вероятностный роевой поиск исключен: транспортным средствам необходима строгая детерминированная направленность к точке назначения (исключающая случайные блуждания). \\ \hline
Алгебраическая маршрутизация (GraphBLAS) & Отказ от объектного представления графов в пользу плоских одномерных массивов формата сжатого хранения разреженных строк (Compressed Sparse Row, CSR), обеспечивающих линейное чтение оперативного запоминающего устройства (ОЗУ) \cite{buluc2017design}. & Матричный подход (SpMSpM / параллельный поиск в ширину (Breadth-First Search, BFS)) вносит избыточные накладные расходы при вычислении индивидуальных коротких маршрутов на разреженных графах по сравнению с локальными методами. \\ \hline
\end{tabularx}
\end{table}

Анализ, представленный в таблице \ref{tab:paradigms_comparison}, показывает, что применение чистых концептуальных парадигм в исходном виде невозможно из-за вычислительных ограничений. По этой причине архитектура разработанного комплекса строится на базе гибридного подхода, объединяющего элементы низкоуровневой оптимизации памяти и модифицированный алгоритм поиска пути:

1. Представление графа сети в памяти (на основе идей GraphBLAS):
Вместо объектно-ориентированного представления графа используется формат сжатого хранения строк (CSR) \cite{drepper2007memory, buluc2017design}. Данные ребер и вершин размещаются в плоских непрерывных массивах. Это исключает разыменование указателей и активирует аппаратный механизм предвыборки процессора. Вычислительно затратные матричные операции GraphBLAS при этом исключены.

2. Кэш-локализация (на основе идей пространственного хеширования):
Пространственное индексирование на базе кодов Мортона (Z-кривой) \cite{morton1966computer} перенесено на этап предобработки. Координаты узлов отображаются в одномерный индекс для физической сортировки структуры CSR в памяти. Это гарантирует размещение топологически близких участков дорог в смежных кэш-линиях процессора, снижая частоту промахов кэша L3 и предотвращая ложное разделение данных между ядрами \cite{butorin2026thread}. Поиск путей по ячейкам координатной сетки не используется из-за несовместимости с правилами маневрирования.

3. Учет динамической загруженности (на основе концепции стигмергии):
Динамические изменения плотности потока фиксируются через механизм временных интервалов резервирования емкости ребер. Запись проезда агента увеличивает плотность потока в соответствующем интервале резервирования. Этот подход является детерминированной реализацией цифровых феромонов роевого интеллекта. Время проезда ребра вычисляется за $O(1)$ без перестройки топологии графа. Вероятностный характер исходной парадигмы роевого поиска заменен строгим детерминированным расчетом.

4. Базовый алгоритм поиска пути (ALT-маршрутизация на основе поиска A*, опорных ориентиров и неравенства треугольника):
Для ускорения поиска путей применен алгоритм ALT (A* search, Landmarks, and Triangle inequality) \cite{goldberg2004alt}. Использование нижних граней расстояний до маяков по неравенству треугольника заменяет евклидову эвристику классического $A^*$. Это сохраняет корректность оценок на направленных графах с учетом задержек на маневры. Данные расстояний упакованы в структуру массивов SoA (Structure of Arrays), что позволяет рассчитывать потенциалы вершин параллельно с помощью векторных инструкций AVX2 (Advanced Vector Extensions 2) \cite{butorin2026thread}. При этом двунаправленный поиск исключен из-за отсутствия временного детерминизма при пошаговом бронировании ребер.


На основе проведенного анализа был сделан вывод о высокой вычислительной сложности классического автономного агентного подхода для поставленной задачи. Попытка индивидуального моделирования поведения каждого агента ведет к значительному снижению производительности.

В связи с этим в разработанной системе обоснован и применен гибридный подход, основанный на декомпозиции вычислительных слоев \cite{butorin2026thread}:
\begin{itemize}
    \item Делегирование функций принятия решений на макроуровень. Агент представляет собой компактную (порядка 100 байт), пассивную структуру данных без индивидуального контура управления, которая следует по заданному пути в соответствии с законами одномерной кинематики.
    \item Вычислительное ядро маршрутизации. Модуль работает без сохранения состояния, выполняя расчет кратчайших путей по зависимым от времени весам ребер графа.
    \item Макроскопический модуль принятия решений. Логика диспетчеризации вынесена на макроуровень, где осуществляется глобальный мониторинг состояния сети и инициируется перерасчет маршрутов при снижении средней скорости движения на сегментах.
\end{itemize}

Такая декомпозиция (пассивный агент~-- бесконтекстный роутер~-- интеллектуальный диспетчер) решает проблему вычислительной сложности и позволяет реализовать алгоритмы диспетчеризации (регулирование пропускной способности, оптимизацию макропараметров) без необходимости автономного обучения каждого агента.

