\section{Сравнительный обзор подходов к агент-ориентированному моделированию и навигации}

При проектировании интеллектуальных навигационных комплексов для современных мегаполисов одной из наиболее острых проблем является выбор правильной архитектурной парадигмы управления транспортными потоками. Дорожная сеть сверхбольшой размерности (например, Московская агломерация содержит порядка 600 тысяч сегментов дорог и 1,5 миллиона возможных маневров) ставит перед исследователями серьезные вычислительные вызовы. В рамках данной научно-исследовательской работы был проведен глубокий сравнительный анализ применимости современных математических и алгоритмических подходов к решению задачи агент-ориентированной навигации и планирования транспортных систем \cite{yakimov2013planning, yakimov2022planning_visum}.

Большинство передовых парадигм (2022–2026 гг.) математически состоятельны, однако их физическая реализация в условиях жестких ограничений по времени отклика (латентности) и объему оперативной памяти невозможна. Результаты отсева представлены в таблице \ref{tab:paradigms_comparison}.

\begin{table}[htbp]
\centering
\small
\renewcommand{\arraystretch}{1.2}
\caption{Анализ применимости современных алгоритмических парадигм маршрутизации}
\label{tab:paradigms_comparison}
\begin{tabularx}{\linewidth}{|p{3.5cm}|X|X|}
\hline
Парадигма & Применимые концепции (Теоретическая польза) & Аппаратный или алгоритмический барьер (Ограничения) \\ \hline
Игры среднего поля (MFG) & Сведение транспортных потоков к строгой непрерывной гидродинамике (сопряженные дифференциальные уравнения) \cite{seaports_mean_field, huang2007large}. & Сложность решения сопряженной системы прямых-обратных дифференциальных уравнений (HJB и FPK). Требуется допущение стационарности, нарушающееся в реальном времени. \\ \hline
Непрерывный MAPF / PIBT & Временная сложность $O(1)$ для однородных сеток (приоритетный сдвиг на роботизированных складах) \cite{stern2019mapf, okumura2019pibt}. & Неприменимость допущения о синхронности шагов на реальном асинхронном графе дорог с разбросом длин ребер. Алгоритмическая NP-трудность поиска оптимального решения. \\ \hline
Обучение с подкреплением (MARL) & Возможность индивидуальной адаптивности агентов к сложным нелинейным заторам \cite{hernandez2017survey}. & Нестационарность среды (цель обучения постоянно смещается), шум одновременного исследования и плохая масштабируемость на крупных дорожных сетях. \\ \hline
Тензорные вычисления (QUBO) & Математически гарантированный поиск системного глобального оптимума \cite{feld2019hybrid}. & Жесткие физические ограничения квантовых процессоров по числу кубитов и разреженной топологии связей (Chimera-граф), требующие эвристической декомпозиции (QBSolv). Высокая задержка удаленных QPU-вызовов по сравнению с локальной CPU-эмуляцией. \\ \hline
Графовые нейросети (PINN/GNN) & Высокая скорость инференса (прямого прохода) для получения готовых маршрутов \cite{luo2024stg4traffic}. & Ограничения выразительной способности в локальных моделях и абсолютная неспособность к онлайн-адаптации: изменение статической топологии (перекрытие дороги) требует полного переобучения модели на GPU-кластере \cite{garg2020generalization}. \\ \hline
Пространственное хеширование & Концепция Z-кривой (коды Мортона) гарантирует, что соседние 2D-узлы лежат в одной кэш-линии процессора \cite{morton1966computer}. & Маршрутизация агентов исключительно по геометрическим ячейкам (Quadtrees) неэффективна на графах со строгими правилами движения. \\ \hline
Роевой интеллект (Стигмергия) & Цифровые феромоны идеально ложатся в концепцию 5-минутных интервалов резервирования (чтение уровня затора за $O(1)$) \cite{deepa2016swarm}. & Вероятностный роевой поиск исключен: транспортным средствам необходима строгая детерминированная направленность к точке назначения (исключающая случайные блуждания). \\ \hline
Алгебраическая маршрутизация (GraphBLAS) & Отказ от объектного представления графов в пользу плоских одномерных массивов формата CSR, обеспечивающих линейное чтение ОЗУ \cite{buluc2017design}. & Матричный подход (SpMSpM / параллельный BFS) вносит избыточные накладные расходы при вычислении индивидуальных коротких маршрутов на разреженных графах по сравнению с локальными методами. \\ \hline
\end{tabularx}
\end{table}

Анализ, представленный в таблице \ref{tab:paradigms_comparison}, показывает, что применение чистых концептуальных парадигм в исходном виде невозможно из-за вычислительных ограничений. По этой причине архитектура разработанного комплекса строится на базе гибридного подхода, объединяющего элементы низкоуровневой оптимизации памяти и модифицированный алгоритм поиска пути:

1. Представление графа сети в памяти (на основе идей GraphBLAS):
Вместо объектно-ориентированного представления графа используется формат сжатого хранения строк (CSR) \cite{drepper2007memory, buluc2017design}. Данные ребер и вершин размещаются в плоских непрерывных массивах. Это исключает разыменование указателей и активирует аппаратный механизм предвыборки процессора. Вычислительно затратные матричные операции GraphBLAS при этом исключены.

2. Кэш-локализация (на основе идей пространственного хеширования):
Пространственное индексирование на базе кодов Мортона (Z-кривой) \cite{morton1966computer} перенесено на этап предобработки. Координаты узлов отображаются в одномерный индекс для физической сортировки структуры CSR в памяти. Это гарантирует размещение топологически близких участков дорог в смежных кэш-линиях процессора, снижая частоту промахов кэша L3 и предотвращая ложное разделение данных между ядрами \cite{butorin2026thread}. Поиск путей по ячейкам координатной сетки не используется из-за несовместимости с правилами маневрирования.

3. Учет динамической загруженности (на основе концепции стигмергии):
Динамические изменения плотности потока фиксируются через механизм временных интервалов резервирования емкости ребер. Запись проезда агента увеличивает плотность потока в соответствующем интервале резервирования. Этот подход является детерминированной реализацией цифровых феромонов роевого интеллекта. Время проезда ребра вычисляется за $O(1)$ без перестройки топологии графа. Вероятностный характер исходной парадигмы роевого поиска заменен строгим детерминированным расчетом.

4. Базовый алгоритм поиска пути (ALT-маршрутизация):
Для ускорения поиска путей применен алгоритм ALT \cite{goldberg2004alt}. Использование нижних граней расстояний до маяков по неравенству треугольника заменяет евклидову эвристику классического $A^*$. Это сохраняет корректность оценок на направленных графах с учетом задержек на маневры. Данные расстояний упакованы в структуру массивов (SoA), что позволяет рассчитывать потенциалы вершин параллельно с помощью инструкций AVX2 \cite{butorin2026thread}. При этом двунаправленный поиск исключен из-за отсутствия временного детерминизма при пошаговом бронировании ребер.


На основе проведенного анализа был сделан вывод о неэффективности классического агентного подхода для поставленной задачи. Попытка наделить каждый элемент потока индивидуальным интеллектом неизбежно ведет к критическому падению производительности.

В связи с этим в разработанной системе обоснован и применен гибридный агент-ориентированный подход, суть которого заключается в строгой декомпозиции вычислительных слоев \cite{butorin2026thread}:
\begin{itemize}
    \item Лишение агента автономии на микроуровне. В разработанной архитектуре агент представляет собой экстремально компактную (порядка 100 байт), пассивную структуру данных — квант транспортного потока. Агент не анализирует обстановку, а лишь строго следует предписанному пути, подчиняясь законам одномерной кинематики.
    \item Высокопроизводительное маршрутизирующее ядро. Роутер реализован как модуль без сохранения состояния, задача которого — максимально быстро находить путь по абстрактным, зависимым от времени весам, без контекста о конкретном агенте.
    \item Макроскопический интеллект (Модуль принятия решений). Логика и интеллект системы вынесены на макро-уровень. Именно модуль принятия решений выступает в роли глобального регулятора, анализирующего состояние всей сети и отправляющего триггерные запросы на пересчет маршрутов для тех агентов, чье плановое время прибытия начинает деградировать из-за формирующихся заторов.
\end{itemize}

Такая декомпозиция (пассивный агент — сверхбыстрый роутер — интеллектуальный диспетчер) решает проблему вычислительной сложности и открывает возможности для будущего внедрения алгоритмов машинного обучения именно на уровне диспетчеризации (регулирование пропускной способности, выставление макро-штрафов), избегая нестационарности среды, свойственной мультиагентным системам с автономным обучением.
