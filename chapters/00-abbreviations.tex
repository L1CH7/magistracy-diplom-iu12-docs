\begin{abbreviations}
    \definition{БД}{база данных}
    \definition{ГИС}{геоинформационная система}
    \definition{МКАД}{Московская кольцевая автомобильная дорога}
    \definition{ПО}{программное обеспечение}
    \definition{СУБД}{система управления базами данных}

    \definition{ALT}{A* search, Landmarks, and Triangle inequality (метод ориентиров и неравенства треугольника)}
    \definition{API}{Application Programming Interface (программный интерфейс приложения)}
    \definition{BBOX}{Bounding Box (ограничивающий прямоугольник)}
    \definition{CH}{Contraction Hierarchies (иерархии сокращений)}
    \definition{CPU}{Central Processing Unit (центральный процессор)}
    \definition{CSV}{Comma-Separated Values (текстовый формат с разделителями)}
    \definition{DDR}{Double Data Rate (синхронная динамическая память с произвольным доступом)}
    \definition{EPSG}{European Petroleum Survey Group (стандарт кодирования систем координат)}
    \definition{ER}{Entity-Relationship (модель «сущность-связь»)}
    \definition{FK}{Foreign Key (внешний ключ)}
    \definition{FPS}{Frames Per Second (кадры в секунду)}
    \definition{GiST}{Generalized Search Tree (обобщенное дерево поиска)}
    \definition{GUI}{Graphical User Interface (графический интерфейс пользователя)}
    \definition{HTTP}{HyperText Transfer Protocol (протокол передачи гипертекста)}
    \definition{I/O}{Input/Output (ввод-вывод)}
    \definition{ID}{Identifier (идентификатор)}
    \definition{IPC}{Inter-Process Communication (межпроцессное взаимодействие)}
    \definition{JS}{JavaScript (язык программирования)}
    \definition{JSON}{JavaScript Object Notation (текстовый формат обмена данными)}
    \definition{KNN}{K-Nearest Neighbors (метод $k$-ближайших соседей)}
    \definition{LOD}{Level of Detail (уровень детализации)}
    \definition{MLD}{Multi-Level Dijkstra (многоуровневый алгоритм Дейкстры)}
    \definition{MVCC}{Multi-Version Concurrency Control (многоверсионное управление параллельным доступом)}
    \definition{MVT}{Mapbox Vector Tiles (векторные тайлы Mapbox)}
    \definition{NVMe}{Non-Volatile Memory Express (спецификация протоколов доступа к твердотельным накопителям)}
    \definition{OSM}{OpenStreetMap (открытый картографический проект)}
    \definition{OSRM}{Open Source Routing Machine (открытый движок маршрутизации)}
    \definition{PCIe}{Peripheral Component Interconnect Express (высокоскоростная шина ввода-вывода)}
    \definition{PDI}{Path Diversity Index (индекс разнообразия путей)}
    \definition{PK}{Primary Key (первичный ключ)}
    \definition{PLG}{Promtail, Loki, Grafana (стек технологий для сбора и анализа логов)}
    \definition{RAM}{Random Access Memory (оперативная память)}
    \definition{RPC}{Remote Procedure Call (удаленный вызов процедур)}
    \definition{SQL}{Structured Query Language (язык структурированных запросов)}
    \definition{SSD}{Solid-State Drive (твердотельный накопитель)}
    \definition{TCP}{Transmission Control Protocol (протокол управления передачей)}
    \definition{TLS}{Transport Layer Security (протокол защиты транспортного уровня)}
    \definition{UI}{User Interface (пользовательский интерфейс)}
    \definition{WebGL}{Web Graphics Library (программная библиотека для 3D-графики в браузере)}
    \definition{WGS}{World Geodetic System (всемирная геодезическая система)}
    \definition{YAML}{YAML Ain't Markup Language (язык сериализации данных)}
\end{abbreviations}